---
import Image from "@/components/ui/Image.astro";

interface Props {
  images: string[];
  alt: string;
}

const { images, alt } = Astro.props;
---
<div class="gallery-grid">
  {images.map((image) => <Image src={image} alt={alt} />)}
</div>
