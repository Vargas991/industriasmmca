---
import { siteConfig } from "@/config/site";
import SocialLinks from "@/components/shared/SocialLinks.astro";
import { formatPhoneForHref } from "@/lib/utils/format";
---
<div class="flow">
  <h2>Contacto directo</h2>
  <!-- <p>{siteConfig.address}</p> -->
  <a href={`tel:${formatPhoneForHref(siteConfig.whatsappNumber)}`}>+58 0412 781 5210</a> <br>
  <a href={`tel:${formatPhoneForHref(siteConfig.whatsappNumber2)}`}>+58 0416 676 4858</a> <br>
  <a href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a>
  <br>
  <SocialLinks />
</div>
